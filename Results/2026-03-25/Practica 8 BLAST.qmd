---
title: "Practica 8 BLAST Lorena Gonzalez"
format: html
---

# Ejercicio 1

Enmascarar e indexar los proteomas

Puedes copiar estas órdenes a tu cuaderno de trabajo. Fíjate que deben ser ejecutadas en la carpeta adecuada.

```{bash}
if [ ! -e ../../data/buchnera.paa ]; then
   # Creamos un archivo para enmascarar zonas de baja complejidad:
   if [ ! -e buchnera_seg.asnb ]; then
      segmasker -in ../../data/buchnera.fasta \
                -infmt fasta \
                -parse_seqids \
                -outfmt maskinfo_asn1_bin \
                -out ../../data/buchnera_seg.asnb
   fi
   # Y indexamos el archivo FASTA para BLAST:
   makeblastdb  -in ../../data/buchnera.fasta \
                -input_type fasta \
                -dbtype prot \
                -parse_seqids \
                -mask_data ../../data/buchnera_seg.asnb \
                -out ../../data/buchnera \
                -title "Proteoma de Buchnera"
fi
```

## Hay que repetir todo esto para Blochmannia.

```{bash}
if [ ! -e ../../data/blochmannia.paa ]; then
   # Creamos un archivo para enmascarar zonas de baja complejidad:
   if [ ! -e blochmannia_seg.asnb ]; then
      segmasker -in ../../data/blochmannia.fasta \
                -infmt fasta \
                -parse_seqids \
                -outfmt maskinfo_asn1_bin \
                -out ../../data/blochmannia_seg.asnb
   fi
   # Y indexamos el archivo FASTA para BLAST:
   makeblastdb  -in ../../data/blochmannia.fasta \
                -input_type fasta \
                -dbtype prot \
                -parse_seqids \
                -mask_data ../../data/blochmannia_seg.asnb \
                -out ../../data/blochmannia \
                -title "Proteoma de Blochmannia"
fi
```
##Realizar las búsquedas de BLASTP

El objetivo es encontrar la proteína de Blochmannia ocreatus que más se parece a cada una de las proteínas de Buchnera aphidicola, y viceversa. Entonces, seleccionaremos las parejas de proteínas que sean recíprocamente las más similares una de otra, por ser más probablemente ortólogas. Otras relaciones de semejanza no recíprocas pueden ser debidas a la paralogía.

De Buchnera aphidicola hacia Blochmannia ocreatus

```{bash}
blastp -query ../../data/buchnera.fasta \
       -db ../../data/blochmannia \
       -out buc2blo.txt \
       -outfmt '6 qseqid sseqid qlen slen length evalue bitscore' \
       -evalue 0.001 \
       -max_target_seqs 1
```

De Blochmannia ocreatus hacia Buchnera aphidicola

```{bash}
blastp -query ../../data/blochmannia.fasta \
       -db ../../data/buchnera \
       -out blo2buc.txt \
       -outfmt '6 qseqid sseqid qlen slen length evalue bitscore' \
       -evalue 0.001 \
       -max_target_seqs 1
```

qseqid y sseqid se refiere a la secuencia de identificación de buchnera y blochmannia respectivamente; luego el length (qlen y slen) y el bitscore
##Comparación de resultados

```{r}
buc2blo <- read.table(
   'buc2blo.txt',
   col.names = c('query', 'subject', 'qlen', 'slen',
                 'length', 'evalue', 'bitscore')
)
blo2buc <- read.table(
   'blo2buc.txt',
   col.names = c('query', 'subject', 'qlen', 'slen',
                 'length', 'evalue', 'bitscore')
)
```

Echa un vistazo a estas tablas de resultados. ¿Sabrías identificar las parejas de proteínas que son recíprocamente la que más se parece una a la otra?

Por tanto, sí podría interpretarlo porque en las tablas obtenidas en cada fila se nos indica un query que sería buchnera sobre un subject que seria blochmannia (en el caso de buc2bio), de manera que tendríamos aquellas proteínas que tienen algo de coincidencia entre ambos organismos.
Lo que se hace es coger el subject del documento buc2bio y compararlo con el query del blo2buc, y eso es mediante la funcion match. Se puede sacar asi la tabla de coincidencia

```{r}
buc2blo$reverse <- blo2buc[
   match(buc2blo$subject, blo2buc$query),
   'subject'
]
coincidencias <- buc2blo[
   buc2blo$query == buc2blo$reverse,
   c('query', 'subject')
]
```
¿Qué datos te gustaría representar gráficamente para ganar confianza en que los resultados son correctos y tienen sentido?

Podríamos comprobar que las parejas en las que realmente coincide, asegurarnos de que los valores E son bajos al menos; tambien que no haya muchos gaps, o que la diferencia en la longitud del alineamiento, o del query y el subject no sean muy diferentes, es algo facil de ver en una distribucion como un histograma.

# Ejercicio 2
El PSI-BLAST nos permite identificar semejanzas sutiles, como las que existen entre miembros alejados de una superfamilia proteica. Para comprobarlo vamos a realizar una búsqueda a partir de cualquiera de las proteínas siguientes: Q9CC91, P39615, B1AJI8, P47343, Q5UPT2, Q6UDG6, A1AFY9, O59825 y P30314. Todas ellas están en Swissprot y pertenecen a una misma superfamilia. Sin embargo, un simple BLASTP con cualquiera de ellas no encuentra ninguna de las otras1. Comprobemos si con un PSI-BLAST lo conseguimos. A continuación lo intento a partir de P39615. Repite los pasos usando otra proteína de la lista como query y compara los resultados.

```{bash}
# Como Swissprot debería estar ya descargada en `../../data`, podemos sacar
# de ahí la secuencia de la proteína escogida:
blastdbcmd -db ../../data/swissprot -entry P39615 -out P39615.fa

# Y ahora la usamos como query en psi-blast:
psiblast -db ../../data/swissprot \
         -query P39615.fa \
         -out P39615.blastout.txt \
         -evalue 0.1 \
         -outfmt 7 \
         -inclusion_ethresh 0.001 \
         -num_iterations 7 \
         -matrix BLOSUM62
```

```{bash}
# Como Swissprot debería estar ya descargada en `../../data`, podemos sacar
# de ahí la secuencia de la proteína escogida:
blastdbcmd -db ../../data/swissprot -entry B1AJI8 -out B1AJI8.fa

# Y ahora la usamos como query en psi-blast:
psiblast -db ../../data/swissprot \
         -query B1AJI8.fa \
         -out B1AJI8.blastout.txt \
         -evalue 0.1 \
         -outfmt 7 \
         -inclusion_ethresh 0.001 \
         -num_iterations 7 \
         -matrix BLOSUM62
```

Consulta la ayuda de psiblast y asegúrate de entender para qué sirven los argumentos que hemos usado. Por ejemplo, ¿cuál es la diferencia entre -evalue y -inclusion_ethresh?¿Cómo harías que la búsqueda fuera más sensible todavía?

-evalue <Real>
   Expectation value (E) threshold for saving hits 
   Default = `10'

-inclusion_ethresh <Real>
   E-value inclusion threshold for pairwise alignments
   Default = `0.002'

Tenmeos una superfamilia y la semejanza entre ellas es basatnte floja, la idea por eso es hacer el PSI BLAST. Se puede automatizar y en un mismo bloque hacer lo mismo para todas; el loop es el siguiente:
el dolar al principio para saber que es una variable, no una cadena de caracteres
for proteina in Q5UPT2 Q6UDG6; do
done

```{bash}
# Como Swissprot debería estar ya descargada en `../../data`, podemos sacar
# de ahí la secuencia de la proteína escogida:
for proteina in Q9CC91, P39615, B1AJI8, P47343, Q5UPT2, Q6UDG6, A1AFY9, O59825, P30314; 
do
blastdbcmd -db ../../data/swissprot -entry $proteina -out $proteina.fa

# Y ahora la usamos como query en psi-blast:
psiblast -db ../../data/swissprot \
         -query B1AJI8.fa \
         -out B1AJI8.blastout.txt \
         -evalue 0.1 \
         -outfmt 7 \
         -inclusion_ethresh 0.001 \
         -num_iterations 7 \
         -matrix BLOSUM62
done
```
Cada vez los valores E son mas pequeños en cada iteración, y al final ha convergido. Tenemos el formato de salida nº7, pero en realidad para leer en r, el formato nº 6 podria haber sido mas favorable, hay una linea que no tiene el mismo nunero de campos, lo mas facil seria poner la almohadilla (rompe un poco la idea de que todo resultado deberia ser automatico, y no modificar nada manualmente). Lo idea seria entonces cambiar al 6

El grepl te da un resultado verdadero o falso; cada una de las proteinas las busca en una lista . Las que no encuentra te da un resultado FALSO